# DriveWise — requirements
# pip install -r requirements.txt

# ── Core ──────────────────────────────────────────
numpy>=1.24
pandas>=2.0
python-dotenv>=1.0

# ── Web UI ────────────────────────────────────────
streamlit>=1.35

# ── LLM (optional — for production answer generation) ──
openai>=1.30
# anthropic>=0.28   # alternative

# ── Embeddings (production) ───────────────────────
sentence-transformers>=2.7   # all-MiniLM-L6-v2
faiss-cpu>=1.8               # vector similarity search

# ── Re-ranking (production) ───────────────────────
# pip install sentence-transformers  (cross-encoder/ms-marco-MiniLM-L-6-v2)

# ── RAG framework (production) ────────────────────
langchain>=0.2
langchain-community>=0.2
langchain-openai>=0.1

# ── Evaluation (production) ───────────────────────
# ragas>=0.1   # pip install ragas